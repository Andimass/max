По обработке ответа от МС хочу свериться.
В схеме ответа (DST.MSNDFL.OPERATIONS_TO_PULS.V1) поле system_client_id обязательное (в required). Сейчас мы отправляем туда СНИЛС и, видимо, получаем его же обратно. После перехода в этом поле будет уходить do_id — соответственно, в ответе МС должна вернуть его эхом.
Хочу убедиться, что:

МС действительно возвращает system_client_id обратно как есть (тем же значением, что приняла), а не подставляет что-то своё;
наша обработка ответа на это поле не завязана — по алгоритму 2.2.2 сверка идёт по epk_id, а не по system_client_id, поэтому смена значения не должна ничего сломать, но хочу подтвердить.

Если поле возвращается эхом и в логике сверки не участвует — со стороны обработки ответа доработок не требуется, меняем только то, что отправляем.
