Сейчас шаг 1 — JOIN с epk_person_relation.snils, и в поле system_client_id уходит СНИЛС. По задаче туда надо класть do_id.
Что упрощает работу: do_id уже хранится в employee_py_nob.do_id — заполняется из resultSet.do_id ещё на этапе получения данных прогонов. То есть никакого дополнительного JOIN ради идентификатора не нужно, берём do_id прямо из той строки, которую и так отправляем. JOIN со snils из алгоритма убирается.
Связка уровней идентификаторов понятна: do_id → person_id (do_person_relation) → epk_id (epk_person_relation). do_id — это уровень ДО (3 уровень), что и требуется
Точка отправки тоже зафиксирована: таблица employee_py_nob (send_status R→P→S, при ошибке E), топик SRC.MSNDFL.OPERATIONS_PULS.V1, ответный DST.MSNDFL.OPERATIONS_TO_PULS.V1.
Два вопроса, которые надо закрыть до финала спецификации:

Тип поля. В JSON-схеме отправки system_client_id сейчас описан как number (max 19 девяток), а do_id — это UUID. UUID в number не влезет и не пройдёт валидацию SEDR. То есть поле надо переводить в string, а это изменение контракта — нужно согласование с МС.
Группировка. Сейчас операции группируются по epk_id, один system_client_id (СНИЛС) на ФЛ. Но в employee_py_nob строки уже хранятся в разрезе do_id, а у одного epk_id может быть несколько do_id. Вопрос: при переходе на do_id мы меняем только значение поля или разворачиваем структуру epk_operations по do_id. От этого зависит объём доработки.
