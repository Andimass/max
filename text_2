По структуре сообщения epk_operations — массив, операции под одним epk_id уже разнесены по элементам, и каждая строка employee_py_nob привязана к своему do_id (связка do_id + ms_base_type + tax_rate + datefor). Логично, что do_id берётся на уровне операции (элемента массива), а не epk_id — тогда несколько do_id дают несколько элементов epk_operations, каждый со своим do_id, и выбор приоритетного не нужен. Правильно ли я понимаю, что маппинг строк employee_py_nob в epk_operations идёт 1:1 по operation_id? Если да — приоритет не требуется; если несколько do_id могут схлопываться в одну операцию — нужно правило выбора
