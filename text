Андрей, привет!

По задаче HRPY-25121 (удаление XML-файла из ФП НДФЛ): мы добавляем новый метод, который пойдёт через СОВУ — `DELETE /ndfl_form/{ndflFormId}` (удаление ПНФ в ФП НДФЛ). Сейчас через СОВУ у нас идёт только отправка (`POST /ndfl_form`) по CR HRPY-16824.

Подскажи, пожалуйста:
1. Нужен ли новый CR на СОВУ под этот метод, или достаточно расширить действующий HRPY-16824?
2. Что нужно передать с нашей стороны, чтобы запрос на удаление прошёл вашу валидацию (схемы запроса/ответа, что-то ещё)?
3. Сертификаты и настройки взаимодействия у нас уже прописаны по отправке — под новый метод нужно ли что-то дополнительно?

Метод синхронный, один файл — один запрос, контракт — по спеке ФП НДФЛ «Метод удаления ПНФ». 
