Д1 — UUID.randomUUID() при каждой отправке. Это свойство самого кода: новый идентификатор на повторе не может быть распознан как тот же. Никакая информация из Confluence этого не отменяет.
Д11 — повторный запуск валидации создаёт новый validation_run, значит новый source_event_id. Если так, критерий «тот же run.id» описывает несуществующий случай. Оговорка: если повторный запуск переиспользует запись прогона — я неправа, и это надо просто проверить.
Д10 — 404 в алгоритме не обработан. Это видно по тексту: ветки есть на 200, 400/422, 401, 500.
Д12 — статусов из Kafka в ТЗ нет ни разу, а в задаче они названы. Либо их сознательно вынесли, либо забыли.
